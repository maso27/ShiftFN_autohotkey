# keyd Support on Linux
Autohotkey is a Windows-based application. A good alternative is *keyd*

# Usage
- In Ubuntu 23+, install via `sudo apt install keyd`
(Other distros will require other methods. Google)
- Paste default.conf in to /etc/keyd
- `sudo systemctl restart keyd`

